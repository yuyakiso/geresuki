<%= render "shared/header" %>
  <div>
    <%= image_tag 'geresuki1', class: 'main-image' %>
  </div>
<div class='gerende-index'>
    <h2 class='title'><%= link_to 'ゲレンデ一覧',root_path %></h2>

    <ul class='item-lists'>
    <% @gerendes.each do |gerende|%>
      <li class='list'>
        <%= link_to gerende.name, gerende_path(gerende.id) %>
      </li>

    <% end %>
  <%= paginate @gerendes, outer_window: 3 %>
</div>
<%= render "shared/footer" %>